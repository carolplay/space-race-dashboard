# 项目脑暴与数据需求文档：地月工业化与 I 型文明演进追踪看板（Project Cislunar-I）

> **文档定位：** 本文档旨在为执行 Agent / 研发团队提供系统级产品定义与数据源全景梳理。核心目标是将“中美深空博弈”与“卡尔达肖夫 I 型文明（Type I Civilization）演进进度”融合，构建一套基于真实物理量与工程数据的追踪体系。

---

## 1. 核心理念与叙事框架

### 1.1 从“冷战象征”到“工业化跃迁”
* **突破 2% 增长瓶颈：** 工业革命以来人类年均 ~2% 的长期生产力增长受限于地球物理承载力与热力学极限。要实现下一次生产力阶跃，必须将能源采集与重工业向大气层外（地月系统）迁移。
* **新太空博弈的本质：** 当前中美太空竞争不再是阿波罗式的“插旗拍照（Flags and Footprints）”，而是一场围绕地外质量通量、稀缺能量源、战略引力节点及原位资源利用（ISRU）的工业基础设施圈地战。
* **双重视角合一：**
  * **地缘视角：** 中美两国在关键深空能力上的双边对照与发展斜率。
  * **物种视角：** 全人类掌控地月空间物质与能量、迈向 I 型文明（K >= 1.0）的实时日志。

### 1.2 顶层合成指标：卡尔达肖夫文明等级读数（K）
基于物理总能流的实时计算：
K = (log10(P_total) - 6) / 10

其中 P_total = P_地表一次能源 + P_天基装机功率 + P_地外工业热能等效（单位：瓦特 W）。当前人类处于约 0.73。

---

## 2. 六大核心指标域（Core Metric Domains）

看板需持续量化并追踪以下 6 个维度的物理与工程指标：

### 维度一：引力势阱穿透与物流吞吐（Mass Flux & Logistics）
* **年入轨净质量（Annual Mass to Orbit）：** 分 LEO、GTO、TLI（地月转移轨道）统计年度总吨位。
* **单位载荷边际发射成本（Marginal Cost per kg）：** 衡量进入太空的平民化与商业化门槛（美元/千克）。
* **运力复用周转率（Reusability & Turnaround）：** 助推器/飞船复用次数、平均周转天数及可复用发射占比（%）。
* **轨道推进剂加注通量（In-Orbit Propellant Transfer）：** 在轨低温推进剂（液氧/甲烷/氢）转运与补加年总吨位。

### 维度二：地外能量捕获与功率密度（Off-Planet Power & Energy）
* **地外总装机电功率（Total Off-Planet Power Generation）：** 在轨空间站、深空飞船及月面基地的光伏与核电总功率（kWe / MWe）。
* **地外核动力部署（Space Nuclear Fission）：** 空间核反应堆电源、核热/核电推进（NTP/NEP）的在轨/月面在役数量与装机容量。
* **天基太阳能电站试验（SBSP Flux）：** 轨道聚光/光伏试验阵列面积（平方米）及微波/激光无线能量传输验证功率。

### 维度三：地月拓扑与战略节点控制（Cislunar Real Estate）
* **月球极区高价值地块占有率（Polar High-Value Sites）：** 沙克尔顿坑、马拉佩特山等南极水冰永久阴影区（PSR）与全日光照高地的实际设施部署点及“安全缓冲区”覆盖面积（平方公里）。
* **地月引力平衡点驻留密度（Lagrange Node Occupancy）：** 地月 L1, L2, L4, L5 及近直线晕轨道（NRHO）、环月冻结轨道（ELFO）上的在轨运行平台数量。
* **地月全域通信与 PNT 覆盖（Cislunar Comm & Navigation）：** 极区高仰角中继覆盖率、星间/星地带宽（Gbps）与月面定位授时精度（米级）。

### 维度四：原位资源利用与生态闭环（ISRU & Biosphere）
* **月面水冰年开采量预估（Water-Ice Extraction）：** 实际开采/提取的 H2O 吨数（推进剂与生命保障源）。
* **生保系统（ECLSS）自给闭环率：** 空间站/月球基地水循环率（%）、氧气再生率（%）及食物卡路里地外自给率（%）。
* **地外制造与非地球质量比（Off-Planet Mass Ratio）：** 月表构筑物中，月壤烧结/制砖自给质量与地球发射上行质量的比例（M_local / M_earth）。

### 维度五：轨道数字化基座与态势感知（Orbital Compute & SSA）
* **巨型互联星座规模（Mega-Constellations）：** LEO 活跃宽带卫星数量（Starlink vs. 千帆/国网等）及 ITU 频轨申报锁定度。
* **天基算力与激光骨干网（Orbital Mesh）：** 星间激光链路总吞吐能力（Pbps）及星载边缘计算总算力（FLOPS）。
* **空间碎片治理（Active Debris Removal）：** 关键轨道碎片清除频次与高精度空间态势感知（SSA）目录数。

### 维度六：深空驻留与地缘协作体系（Human Presence & Governance）
* **人类地外常驻活动量（Off-Planet Human-Days）：** 年度地外宇航员总人数、单次驻留时长、月面作业人·时数。
* **阵营覆盖体量（Alliance Index）：** 《阿尔忒弥斯协定》（Artemis Accords）签约国 vs. 国际月球科研站（ILRS）合作国的 GDP 总量占比与机构数。

---

## 3. 数据来源与字段映射（Data Source Ecosystem）

整个系统需接入公开、权威的跨国遥测、目录与公报数据：

| 模块 / 领域 | 推荐数据源 | 核心数据字段 | 采集模式 |
| :--- | :--- | :--- | :--- |
| 发射与载荷明细 | • Jonathan McDowell (JSR/GCAT)<br>• Launch Library 2 (The Space Devs)<br>• Gunter's Space Page | 发射时间、火箭型号、助推器复用轮次、有效载荷干质量（Dry Mass）、载荷类别、目标轨道类型 | 每日增量抓取 / 任务事件驱动 |
| 空间目标与轨道根数 | • Space-Track.org (18th SDS)<br>• CelesTrak (Dr. T.S. Kelso) | NORAD Catalog ID、国际代号（COSPAR）、轨道六根数（TLE/OMM）、雷达散射截面（RCS）、再入衰减日期 | 周期性 API 轮询（1~4 小时） |
| 国际频轨申报 | • 国际电信联盟 (ITU BR IFIC / SNFN) | 卫星网络名称、轨道位置（经度/平面）、使用频段（Ka/Ku/Q/V）、申报国家/行政机构 | 每半月同步 ITU 官方通报数据 |
| 深空与月球探索 | • NASA Planetary Data System (PDS)<br>• 中国探月工程 / 探月与航天工程中心公报<br>• The Planetary Society Moon Tracker | 月球着陆器坐标（经纬度/撞击坑名）、巡视器里程、水冰采样/挥发分测量值、中继星轨道参数 | 任务发布驱动 / 官方白皮书抽取 |
| 宏观能源与基准 | • Energy Institute (原 BP) Statistical Review<br>• IEA (国际能源署) | 全球地表一次能源年消耗总量（EJ/TWh），作为卡尔达肖夫常数 P_Earth 的计算基准 | 年度更新 |
| 国际公约与条约 | • UN COPUOS (联合国外空司)<br>• NASA Artemis Accords 官方发布 | 签约国清单、签署日期、外空物体登记册（Registration Convention） | 动态爬虫监控 |

---

## 4. 关键指标建模与估算逻辑（Domain Modeling Logic）

执行 Agent 在构建数据转换层时，需处理以下估算与推演逻辑：

### 4.1 涉密 / 非公开载荷质量推演
对于军用或未公开干质量的载荷，建立确定性分级插补模型：
1. 一级匹配：若已知卫星标准平台型号（如东方红四号、波音 702 等），取平台标称平均干质量。
2. 二级推演（运力包线法）：基于火箭型号、发射场纬度与目标轨道（如 200km LEO、SSO、GTO），获取该运载火箭的最大理论运力 Cap_LV。涉密有效载荷质量推定为：
   M_payload = 0.68 * Cap_LV(Target Orbit)

### 4.2 在轨供电功率的物理衰减模型
在轨航天器的太阳能电池阵列受高能带电粒子辐射影响持续衰减：
P(t) = SUM [ P0_i * (1 - delta_i)^delta_t_i ] + SUM [ P_nuclear_j ]
* 砷化镓（GaAs）高效电池年衰减率 delta 约 1.0%；硅基光伏电池 delta 约 2.5%。
* 航天器一旦在 Space-Track 中被标记为 DECAYED（坠毁/再入），自动从装机池中剔除。

### 4.3 稀缺轨道与地形占有评分
针对月球南极与地月关键轨道的占有度，设计加权评分算法：
* 月球南极极昼山脊（80%+ 光照）：极高权重分配（按已部署固定站点的安全缓冲区面积折算）。
* 永久阴影区（PSR 水冰富集坑底）：按实际具备原位勘探能力的探测器点位赋分。
* 地月共振轨道（NRHO / ELFO / L点）：按在轨活跃中继与控制节点数量赋分。

---

## 5. 看板界面布局建议（UI/UX Blueprint）

* **顶部全局状态条（Top Gauge）：**
  * 实时卡尔达肖夫指数：K = 0.73042（年变动率 +0.00018/年，当前所处阶段：近地轨道工业化 LEO）。
* **模块 A（双边能力雷达对比）：**
  * 运力吞吐、地外能源、地月领地、原位利用、轨道算力五维雷达图。
* **模块 B（历史运力通量堆叠图）：**
  * 1957 年至今美国、中国及全球其他国家入轨吨位演进。
* **模块 C（地月空间 3D 拓扑沙盘）：**
  * 动态标定月球南极主要撞击坑（Shackleton, Amundsen 等）实际探测器分布。
  * 实时投射 Gateway (NRHO) 与 鹊桥二号 (ELFO) 的在轨空间轨道态势。
* **模块 D（ISRU 与工业化进度条）：**
  * 水冰开采量、月面 3D 打印面积、受控生态循环自给率。
* **模块 E（地缘与治理阵营）：**
  * Artemis 协定与 ILRS 国际月球科研站参与国版图与 GDP 占比。

---

## 6. 给执行 Agent 的任务分解建议

1. **阶段一（Data Extraction & Schema）：**
   * 接入 JSR (Jonathan's Space Report) 发射历史 TSV 与 Space-Track API。
   * 建立统一的实体关系模型（火箭型号、发射事件、卫星平台、国家归属、轨道类型）。
2. **阶段二（Metric Engine & Calculations）：**
   * 实现年度入轨质量自动统计逻辑（含涉密载荷推演算法）。
   * 实现卡尔达肖夫文明等级指数 K 的时间序列聚合器。
   * 建立月球任务专题表（着陆坐标、中继轨道类型、水冰探测载荷）。
3. **阶段三（Presentation Layer）：**
   * 输出面向前端看板的聚合 API（按国家、年份、指标类别过滤）。
   * 生成清晰的图表数据结构（时序折线、双边雷达图、极区点位热力图）。

---

## 7. Alpha 0.1 执行记录（2026-08-18）

### 评估结论

产品叙事、指标框架与界面模块足以支持首版实现；但原始定义把产品原型、实时数据工程、跨源实体解析和预测模型放在了同一交付层级。Space-Track 账号、ITU 公报解析、载荷质量核验与中国任务非结构化公报抽取均需要独立的数据工程周期，不能在没有数据契约和证据等级的情况下宣称“实时”。

### Alpha 0.1 已确认范围

* 交付单页交互看板，覆盖卡尔达肖夫指数、双边能力、历史运力、地月拓扑、六大指标域、ISRU 进度与数据方法。
* 建立 A–D 四级证据协议；当前界面所有业务读数均标注为 D 级“产品样例”。
* 中美能力切换、NRHO / ELFO 轨道切换、响应式布局与无障碍标签纳入首版。
* 暂不把未经核验的样例读数包装为实时统计，不在 Alpha 0.1 接入账号型或付费数据源。

### 关键取舍与原因

* 以“能力条 + 物理量”替代单一雷达图：更适合同时展示指数和实际工程口径，也更便于移动端阅读。
* 以可解释的二维地月拓扑替代完整 3D 沙盘：首版优先验证节点叙事和交互，后续再引入轨道历元与三维渲染。
* 卡尔达肖夫指数保留为顶层叙事读数，但必须同时展示公式、数据版本与估算标记。

### 验收标准

* 桌面与移动端均能完整浏览核心模块；交互按钮可通过键盘操作。
* 所有演示数据均有显著的样例/估算标识，页面不暗示已完成实时数据接入。
* 生产构建通过；页面可独立部署并为后续 API 替换保留清晰组件边界。

### 下一阶段开放问题

* 确认首批生产数据的“权威源优先级”和授权方式，建议从 Launch Library 2、CelesTrak、Energy Institute 三条低摩擦链路开始。
* 明确双边归属规则：运营主体、发射国、制造国和载荷所有国发生冲突时采用何种主键。
* 对涉密载荷的 `0.68 × 运力包线` 模型开展历史回测，并把点估计改为区间和置信度。
* 确定真实数据更新频率、失败回退策略、审计日志与人工校正流程。

---

## 8. 已确认设计方向：时间维度（Alpha 0.2）

> 状态：方向已确认；具体交互将在真实数据结构确定后实施。

### 核心问题

看板应只回答“今年发生了什么”，还是允许用户把整套状态回拨到历史年份。单纯选择其中一种都会损失另一种价值：只看当年缺少发展斜率，默认展示全历史则会削弱任务控制台的即时性。

### 确认方案

采用“当年态势为默认、历史回溯为第二层”的混合结构：

* 默认模式为“本年”，展示最新值、同比、数据截止日和证据等级。
* 提供“近 5 年 / 全历史”两档回溯；进入回溯后出现统一年份滑杆，驱动顶部读数与双边能力同步变化。
* 运力等历史图表保留独立时间轴，用于观察长期斜率，不强迫所有模块使用相同起始年。
* 未完整结束的当年只与上年同期比较；全年预测值单独标记为 Forecast，不与 YTD 实测值混合。

### 原因

* 产品首要任务仍是回答“文明当前处于哪里”，因此当前态势应保持第一视觉层级。
* 地月工业化的核心价值来自斜率和阶段变化，历史能力不可省略。
* 六个指标域的有效历史起点不同；统一从 1957 年展示会产生大量无意义空值或误导性的零值。

### 待确认细节

* 全历史模式采用自然年份连续回拨，还是采用“1957 / 1969 / 2000 / 2015 / 现在”等里程碑节点。
* 双边能力指数是否允许历史重算；若数据不完整，是否只展示有证据支持的物理量。
* Alpha 0.2 是否先实现“本年 + 近 5 年”，把完整历史回拨留到真实数据管道完成后。

---

## 9. 已确认设计方向：叙事层级与中美直接对比（Alpha 0.2）

### 决策

* 首屏核心问题从“人类距离 I 型文明还有多远”调整为“谁先建成地月工业链”。
* 中美综合分数和六个指标域必须在同一视野内直接并列，不通过国家 Tab 切换。
* 卡尔达肖夫指数从首屏移至指标体系之后，作为运力、能源和工业能力长期积累的最终结果。

### 原因

* 当前产品的主要观察对象是中美地月工业能力差距及变化斜率，文明指数是长期汇聚指标，不应抢占即时决策焦点。
* Tab 切换依赖用户记忆前一个国家的状态，削弱了“比较”本身；同口径、同行展示更适合发现差距和反转项。
* 叙事顺序应从竞争主体出发，经过运力、节点、资源等可操作指标，最终收束到全人类文明进阶。

### Alpha 0.2 验收标准

* 首屏无需交互即可同时看到美国、中国的综合指数和六域分数。
* 详细对比区按指标逐行并列两国的工程读数、指数与差值。
* 卡尔达肖夫指数保留完整公式、进度与阶段信息，但出现在工业化指标之后。

---

## 10. 已确认设计方向：取消能力评分，采用原始量时序面板（Alpha 0.3）

### 决策

* 删除中美综合工业能力指数、六域归一化分数和差值分数；不得用人为权重把异质量纲压缩成单一排名。
* 中美对比只展示原始工程量及其单位，例如吨、MW、活跃节点数、任务数、卫星数和人·日。
* 六项核心对比指标分别使用独立连续时间序列面板，采用类似 Grafana 的监控布局；两国曲线在每个面板内共享同一纵轴。
* 支持“近 5 年 / 全部可用”时间范围切换。每个指标允许采用自己的有效起始年份，不以无意义零值强行补齐。
* 卡尔达肖夫 K 值不属于人为能力打分，继续作为全人类总能流的最终物理指标保留。
* 站点访问模式由私有改为公开。

### 原因

* 综合分依赖任意归一化边界和权重，无法直接审计，也会掩盖真实工程差距。
* 连续时序能同时回答当前值、差距大小、增长斜率和潜在交叉点，信息密度高于静态对比表。
* 独立面板允许每项指标使用合适的单位、量程、更新频率和证据说明。

### Alpha 0.3 验收标准

* 页面不再出现 83.0、63.5 或 0–100 能力评分。
* 六项中美指标均以独立图表呈现，包含原始单位、当前值、时间轴、两国图例和数据等级。
* 时间范围控制能够同步更新全部六个面板。
* 公开网址无需被加入访问名单即可打开。

---

## 11. 已确认指标扩展：发射工业与全球视角（Alpha 0.4）

> 状态：已确认并进入实现。

### 阶段适配原则

* 已进入规模运营的能力使用连续时序图，例如发射、复用、星座部署和在轨驻留。
* 尚未发生或仅有孤立验证的能力不绘制长期零值曲线；改用“未验证 / 飞行验证 / 预运营 / 运营”状态、下一里程碑和事件时间线。
* 百分比、均值、中位数和滚动窗口可作为可复算的运营统计；禁止重新引入主观权重综合分。

### 建议优先接入的发射指标

#### 发射活动量

* 轨道发射尝试次数（次/月、次/年）
* 成功入轨次数（次/月、次/年）
* 30 日滚动发射频次（次/30 天）
* 连续两次发射的中位间隔（天）
* 12 个月滚动任务成功率（%）

#### 复用工业化

* 使用飞行验证一级的发射占比（%）
* 一级回收尝试次数与成功率（次、%）
* 同一助推器两次任务的中位周转时间（天）
* 复用任务的助推器中位飞行轮次（第 N 飞）
* 最高已验证复用轮次（第 N 飞，仅作边界值，不替代中位数）

#### 物流产出

* 年度入轨净质量（吨）
* 单次成功任务平均入轨质量（吨/次）
* LEO / GTO / TLI / 逃逸轨道质量与任务次数
* 深空出发任务次数及有效载荷质量
* 载人和货运发射次数

#### 基础设施吞吐与韧性

* 活跃运载火箭家族数（个）
* 活跃发射台数（个）
* 单发射台年任务数（次/台·年）
* 最大运营商发射占比（%，用于观察集中度）
* 前三大运营商发射占比（%）

### 数据可得性分级

* A 级直接事件字段：尝试次数、成功次数、日期、任务轨道、发射台、服务商。
* B 级需实体关联：助推器序列号、飞行轮次、复用状态、回收结果、周转天数、载荷质量。
* C 级模型字段：涉密载荷质量、实际边际成本、运力利用率；必须显示区间，不作为首批上线指标。

### 全球与“其他”口径建议

* 每个发射时序面板同时显示美国、中国、其他和全球四条序列。
* 发射次数、运力、复用和周转按发射服务商/运载工具控制主体归属，不按发射场地理位置或载荷客户归属。
* 卫星数量、在轨算力等载荷侧指标按载荷运营主体归属；同一页面不得混用两种归属规则。
* 全球值为美国、中国、其他之和；总量型指标优先使用三方堆叠并以顶部边界表达全球总量，比例型指标使用加权全球曲线。
* “其他”先作为聚合序列，后续可展开欧洲、俄罗斯、印度、日本和其他商业主体。

### Alpha 0.4 首批九图

1. 轨道发射尝试次数
2. 成功入轨次数
3. 年度入轨质量
4. 单次任务平均入轨质量
5. 飞行验证一级使用率
6. 一级回收成功率
7. 助推器中位周转时间
8. 助推器中位飞行轮次
9. 平均单位重量入轨成本（恒定 2026 USD/kg，C 级模型估算）

### 单位重量入轨成本口径

* 计算式：`Σ 估算任务价格 / Σ 实际或推定入轨质量`。
* 所有金额换算为恒定 2026 美元；按发射服务商控制主体归入美国、中国或其他。
* 公开合同价格缺失时使用运载工具公开标价或区间中值，并把结果标记为 C 级。
* 该指标反映平均市场准入成本，不等同于发射商内部边际成本；目的轨道差异将在后续版本引入等效质量修正。

### 卡尔达肖夫指数时序化

* 保留当前 K 值，同时增加 1965 年至今的全球年度/关键年份曲线。
* K 时序只展示全球序列，不按国家拆分，因为其定义衡量整个人类可用总能流。
* 历史值由全球一次能源平均功率和已验证的地外功率项统一重算，避免跨版本常数不一致。

### 参考数据入口

* Launch Library 2：发射事件、服务商、火箭构型、发射台、任务轨道与可复用一级记录。
* FAA Commercial Space Data：美国商业发射、再入、许可证及运营统计的校验源。
* 运营商官方任务记录：助推器轮次、回收和复用事实的一级来源。

---

## 12. 已确认架构方向：GitHub 源码管理与托管分工

> 状态：源码已同步至 GitHub 私有仓库；GitHub Pages 暂不迁移。

### 决策

* GitHub 作为源码主仓库，保留 `main` 分支的完整提交历史；仓库先按私有创建，后续可随时改为公开。
* 现有 Sites 继续承担公开生产部署，不改变当前公开网址。
* 当前不迁移到 GitHub Pages，避免同时维护两套发布路径。

### 原因

* 当前项目采用 vinext 和 Cloudflare Worker 兼容运行结构，并非单纯的静态文件站点。
* 迁移 GitHub Pages 需要增加静态导出和 GitHub Actions 发布流程；未来接入数据 API、缓存、认证或持久化后还会受到纯静态托管限制。
* 源码管理与生产托管解耦后，GitHub 负责版本、协作和回滚，Sites 负责构建与公开访问，职责更清晰。

### 重新评估 GitHub Pages 的触发条件

* 产品长期确定为纯静态站点，不需要服务端运行时或数据接口。
* 团队希望把全部发布控制统一迁入 GitHub Actions。
* 现有 Sites 在自定义域名、可用性或访问策略上不再满足需求。

---

## 13. 已确认数据方向：月更快照与首批真实指标（Alpha 0.5）

> 状态：已实现并提交公开仓库；待随下一生产版本发布。

### 决策

* 数据不要求实时，大部分来源采用每月一次的 ad hoc 拉取。
* 首批真实化指标为“轨道发射尝试次数”和“成功入轨任务数”；两者直接由任务事件聚合，暂不同时推进质量、复用和成本模型。
* 初期使用 Git 跟踪的 JSON 任务快照和派生指标，不启用 D1。这样每次更新都有 diff、提交记录和可复算输入。
* 看板允许真实指标和产品样例并存，但必须在每个面板明确标注 `真实事件` 或 `产品样例`，不得让样例值伪装成观测值。

### 首批来源与口径

* 来源：The Space Devs 的 Launch Library 2，生产 API v2.3.0。
* 范围：仅轨道任务；终态 ID 3、4、7 计为发射尝试，仅 ID 3 计为成功。
* 归属：优先使用发射服务商在发射台 agency 数据中的国家；字段不可得时退回发射台国家，并在任务记录中保留 `classificationBasis` 供审计。
* 证据等级：B 级结构化公共数据；在与运营商或官方年报交叉校验前不升为 A 级。

### 更新与存储

* `data/snapshots/launch-library-2.json` 保存任务级规范化记录和来源链接。
* `data/metrics/launch-activity.json` 保存看板直接读取的年度聚合。
* `npm run data:update:launches -- --from=YYYY-MM-DD --to=YYYY-MM-DD` 执行指定窗口更新。
* 无参数运行时覆盖上一个自然月至当天，以吸收迟到的状态更正；窗口内旧记录被替换，窗口外历史记录保留。

### Alpha 0.5 验收标准

* GitHub 仓库为公开可读。
* 至少两项发射指标从持久化任务快照生成，不再引用页面内硬编码样例。
* 每条任务保留来源 ID、时间、状态、服务商、国家归属、归属依据和源链接。
* 页面显示真实数据截止日期、真实指标数量和样例指标数量。
* 更新脚本、数据口径和速率限制注意事项在仓库内可查。

### 后续顺序

1. 扩充历史发射事件覆盖范围并与官方年度汇总交叉核验。
2. 基于一级序列号接入复飞率、回收率和周转时间。
3. 接入载荷质量后生成年度入轨质量和单次平均质量。
4. 最后建立价格区间、通胀换算和轨道修正后的单位重量成本模型。

---

## 14. 已确认信息架构：四大观测模块（Alpha 0.6）

> 状态：Alpha 0.6 已实现并通过本地构建；待保存 Sites 版本并确认公开发布。

### 页面结构

看板改为四个连续展示的一级模块，不使用需要切换后才能看到内容的 tab：

1. 在轨资产（ORBITAL ASSETS）
2. 发射工业（LAUNCH INDUSTRY）
3. 地月专题（CISLUNAR FRONTIER）
4. 空间站（SPACE STATIONS）

方法论和卡尔达肖夫指数保留在四大模块之后。中美对比继续作为主要视角；全球和其他主体作为同图参照。

### 14.1 在轨资产

#### 核心问题

* 目前有多少有效航天器在轨？总质量是多少？
* 这些资产承担什么功能，分布在哪些轨道？
* 美国、中国和其他主体的存量、结构与增量有何差异？

#### 指标

* 在轨有效载荷数量：主指标，只统计仍在轨的 payload；同时标注其中可确认仍在工作的数量。
* 全部目录对象数量：有效载荷、火箭体、任务相关物体和碎片，作为轨道环境背景，不与有效资产混为一谈。
* 在轨有效载荷质量：已知质量之和，并同时显示“已知质量对象数 / 总对象数”和覆盖率；不得把缺失质量当作零。
* 全部在轨物体质量：优先采用 ESA 汇总口径作为校验，不直接假设对象级质量字段完整。
* 卫星类型：通信、地球观测、导航定位、科学、技术验证、载人/空间站、军事/未公开、其他。
* 轨道分布：LEO、MEO、GEO、HEO、地月/深空、其他；LEO 进一步按高度带展示拥挤度。

#### 展示方式

* 顶部 KPI：有效载荷数、已确认运行数、已知质量、质量覆盖率。
* 数量和质量使用月度快照时序图。
* 卫星类型使用按中美/其他分组的堆叠条形或矩形树图。
* 轨道分布使用轨道带图：按高度/轨道类别分层，数量与质量可切换，但不使用国家 tab。

#### 数据入口

* CelesTrak SATCAT：当前在轨状态、对象类型、所有者、近地点、远地点、倾角和轨道中心。
* GCAT：对象级质量、类型、状态和非地球轨道对象补充。
* ESA Space Environment Statistics / Annual Report：全球对象数量和总在轨质量的汇总校验。
* 每月保存独立日期快照，例如 `data/snapshots/orbit-assets/YYYY-MM-DD.json`；不能只依赖 Git 历史生成线上时序。

### 14.2 发射工业

#### 保留指标

* 轨道发射尝试、成功入轨任务、成功率。
* 年度入轨质量、平均任务质量、复用率、回收率、周转时间和单位重量成本按数据成熟度逐步替换样例。

#### 新增火箭类型数据

* 火箭家族年度任务数和份额。
* 具体构型年度任务数、成功数和成功率。
* 活跃火箭家族数量与新构型首飞事件。
* 可复用 / 部分复用 / 一次性构型占比。
* 轻型 / 中型 / 重型 / 超重型运力等级；该分类需基于统一的标称 LEO 运力阈值，不能按营销名称判断。

#### 展示方式

* 继续保留已有总量时序小图。
* 增加火箭家族排名条形图，以及中美各自构型组合的 100% 堆叠图。
* 首飞、退役和重大失利作为事件标记叠加，不另造一条稀疏时序。

#### 数据实现

* 从现有 Launch Library 2 任务快照补存 `rocket configuration id`、全名和 family。
* 构型制造商、复用方式和标称运力从 launcher configuration 资料表补充，按月或在新构型出现时 ad hoc 更新。

### 14.3 地月专题

地月能力尚未形成稳定连续运营，不制作伪时序图，采用静态状态与事件展示。

#### 展示内容

* 当前运行资产：月球轨道器、月面着陆器/巡视器、通信中继和地月空间节点。
* 地理/拓扑：月面近侧、远侧、南极，月球轨道，EM-L1 / EM-L2，NRHO 等。
* 项目状态：规划、已批准、研制中、已发射、运行中、完成、失效、取消、政策复审。
* 中美项目矩阵：任务目标、运营主体、当前位置、当前状态、下一验证门槛。
* 过去 12 个月与未来 24 个月事件流：发射、着陆、采样返回、节点部署和政策变化。

#### 规则

* “计划日期”与“已发生日期”必须视觉区分。
* 预算提案、机构任务页和实际拨款出现冲突时标记“政策复审”，不得直接标记取消或确定执行。
* 每张状态卡必须有 `asOf`、来源链接和最后核验日期。

### 14.4 空间站

空间站与地月专题一样，以当前状态、结构参数和事件流为主，不强制长期时序。

#### 当前运行站

* 国际空间站（ISS）
* 中国空间站（Tiangong）

#### 每站展示字段

* 官方实景图或官方结构图，并保留来源和版权说明。
* 在轨状态、运营主体、首次入轨、连续驻留起点。
* 质量、轨道高度、倾角、增压容积、发电功率、额定乘员、当前对接口/模块。
* 当前乘组与来访飞船只在有稳定来源时展示；否则使用最近核验日期，不标成实时。
* 最近与下一事件：乘组轮换、货运补给、舱段扩展、舱外活动和退役/离轨节点。

#### 规划中空间站

* 商业站和其他国家规划站与运行站分区展示，只显示已签约或有硬件进展的里程碑。
* Gateway 归入地月专题，避免在空间站与地月模块重复统计。

### 数据与图片策略

* 在轨资产与发射工业：机器可读来源，按月运行适配器并保存快照。
* 地月与空间站：仓库内维护人工审核的结构化 JSON，每月复核，重大事件发生时 ad hoc 更新。
* 图片优先使用 NASA、ESA、CNSA/CMSE 等机构官方素材；下载到 `public/` 固化，不依赖第三方热链，并在数据记录中保存 credit 和 source URL。

### Alpha 0.6 实施顺序

1. 扩充 LL2 快照的火箭构型字段，并上线火箭家族排名。
2. 建立 CelesTrak 月度在轨快照，先上线数量、对象类型和轨道分布。
3. 接入 GCAT 质量字段和 ESA 汇总校验，上线质量覆盖率。
4. 建立地月与空间站人工审核数据文件、事件卡和官方图片。
5. 最后重组首页导航与模块顺序，统一真实/样例/计划值标识。

### 开放问题

* “卫星类型”是以任务主用途还是运营商业务类型为准；建议以任务主用途为主，并允许多用途对象保留一个次级标签。
* 军事/未公开卫星是否单列；建议单列，避免强行推断具体用途。
* 空间站是否显示当前乘组名单；建议首版不显示，以降低高频维护成本。

### Alpha 0.6 实施记录（2026-08-19）

* 首页已重组为四个连续模块：在轨资产、发射工业、地月专题、空间站；没有使用内容切换 tab。卡尔达肖夫指数和方法论位于四模块之后。
* 建立 `scripts/update-orbit-assets.mjs`，从 GCAT `active.tsv` 与 `currentcat.tsv` 生成带来源 URL、版本、许可证和 SHA-256 的日期快照，并将全部月度快照聚合到 `data/metrics/orbit-assets.json`。
* 首个轨道快照日期为 2026-08-19：16,624 个活跃载荷、34,950 个当前目录对象、11,272.9 吨已知活跃载荷质量；质量字段覆盖按 16,618 / 16,624 展示，不用四舍五入百分比代替原始分子分母。
* 轨道模块上线 LEO / MEO / GEO / HEO 分布、八类任务主用途、目录对象构成，以及美国、中国、其他和全球的同屏分段比较。月度历史仅从首个真实快照开始积累，不回填推造历史。
* 发射任务快照已扩充火箭 configuration、全名和 family 字段，派生数据增加 2026 年火箭家族 / 构型排名。因本次 LL2 刷新遇到 HTTP 429，首版从已有任务名的构型前缀离线归并；下一次月更优先用正式 configuration ID 回填并替换该降级分类。
* 地月与空间站人工审核数据保存在 `data/editorial/frontier.json`。地月展示运行节点与已发生 / 计划 / 政策复审事件；空间站展示 ISS 与中国空间站的状态和结构参数，首版不维护实时乘组名单。
* ISS 与中国空间站图片已下载到 `public/iss.jpg` 和 `public/tiangong.jpg`，分别保留 NASA 和 China Manned Space 图片 credit，不依赖第三方热链。

### 相对原实施顺序的调整

* 首版在轨数据采用 GCAT 同时覆盖状态、类型、质量和轨道字段，没有先引入 CelesTrak 再二次拼接。ESA 汇总仍作为全部在轨物体质量的独立交叉参照。后续若需要 LEO 高度带、NORAD 编号一致性或更高频状态更新，再补 CelesTrak 适配器。
* “活跃载荷数”直接使用 GCAT Active Catalog；当前不另造“已确认运行数”子指标，避免在来源没有统一运营状态定义时制造伪精度。

---

## 15. 已确认展示与历史方向：双语、十五年序列与双计划轴（Alpha 0.7）

> 状态：已实现，完成测试后公开发布。

### 双语与语言一致性

* 页头提供中文 / English 即时切换，默认中文；切换时同步页面语言属性。
* 所有导航、标题、解释、数据状态、方法说明、地月事件和空间站字段使用同一套语言，不采用一句中混排中英文标签的方式表达翻译。
* US、CN、GCAT、LL2、Artemis 等国家缩写、数据源名和项目专名保持原文，避免错误翻译。

### 2011 至今的连续历史

* 发射尝试和成功任务改用 GCAT LaunchLog 从 2011 年统一重算至当前来源截止日；每年按美国、中国、其他和全球四条序列展示。
* 发射以唯一 `Launch_Tag` 去重；轨道 / 深空发射代码计入尝试，带失败标记者不计成功。当前年份明确标为 YTD，不与完整自然年混用。
* 在轨历史由 GCAT SATCAT 主目录按每年末重建：对象在分离 / 进入目录后且尚未再入，才计入该年库存。
* 历史“载荷对象”包含仍在轨的活跃与失效载荷；当前静态 KPI 使用 Active Catalog。页面明确并列两个定义，不把历史库存冒充当前有效资产。
* 历史已知质量仅汇总有质量字段的 payload；缺失值不作零值处理。源文件 SHA-256、生成时间和全部分类口径写入派生 JSON。

### 当前数字 + 时序图

* 在轨资产先显示当前 Active Catalog 静态 KPI，再显示年末载荷对象数和已知载荷质量两张 2011—当前时序图，随后展示当前轨道、用途和对象构成。
* 发射工业先显示当前 LL2 尝试、成功、成功率与活跃构型，再显示 GCAT 十五年发射曲线和当前火箭构型排名。
* 五年 / 2011 至今范围控制同时作用于在轨与发射历史，使同一页面的时间窗口保持一致。

### 地月资产与中美双时间轴

* 现存资产区使用官方图片展示 LRO、鹊桥二号、嫦娥四号 / 玉兔二号，并保留图片 credit、运行起点、位置、状态、下一节点及官方来源。
* 计划区分为美国 Artemis 与中国探月工程两条独立时间轴，不把两国节点混成单一事件流。
* 节点使用“已完成 / 进行中 / 计划 / 政策复审”状态色；日期语义与事实来源一并存入人工审核数据。
* 美国轴按 2026 年最新官方架构反映 Artemis II 已完成，以及 Artemis III、IV、V 的更新安排；Gateway 标记政策复审。中国轴记录 2026 年载人月球探测飞行试验、嫦娥七号、嫦娥八号和 2030 年前载人登月目标。

### 下一阶段：数据质量

1. 在 LL2 速率限制窗口允许时，用正式 rocket configuration ID 和 family 回填当前按任务名前缀归并的火箭构型。
2. 对当前自然年的 LL2 与 GCAT 任务差异建立逐项对账表，区分来源截止时间、亚轨道 / 轨道边界和状态更新延迟。
3. 评估引入 GCAT 辅助目录，以解释历史主目录与 Current Catalog 在当前总对象数上的覆盖差异。
4. 逐项为入轨质量、复用、回收、周转和单位重量成本建立任务级来源管线，再将相应面板从“产品样例”升级为“观测 / 模型值”。
5. 地月和空间站继续每月复核，并在实际发射、着陆、取消或架构调整时 ad hoc 更新；未来计划不得自动写成已发生事实。

---

## 16. 已确认分层：研发火箭与共享地月时间轴（Alpha 0.8）

> 状态：已实现，测试通过后发布。

### 研发火箭不并入正式运载产能

* “轨道发射尝试 / 成功任务”继续只回答已经发生的轨道和深空运载活动，不为了展示热门项目而放宽口径。
* 新增独立的“研发飞行与工程验证”层，首批覆盖 Starship / Super Heavy、朱雀三号和长征十号系列。
* 研发项目不使用一个虚构综合分数，改用六个可审计能力节点：地面系统、垂直起降、全系统飞行、进入轨道、一级回收、常态载荷。
* 六个节点彼此独立，不强迫线性成熟顺序。例如完成一级捕获不代表已完成轨道载荷交付；试验飞行入轨也不代表已形成常态服务。
* 每个项目展示当前阶段、已完成节点、下一门槛和逐条验证记录。Starship 明确展示 12 次综合飞行试验，同时标明尚未开始常态载荷入轨。
* 一次研发飞行若同时完成轨道任务，可以出现在正式发射事件统计和研发里程碑两处，但两者只是不同观察维度，不得相加生成第二套发射总量。

### 地月专题使用一条共享时间轴

* 原有两条各自滚动的计划轴改为一条中央日期轴：美国事件在左，中国事件在右，同一日期行严格横向对齐。
* 时间轴同时包含关键已执行步骤、当前准备、未来计划和政策复审，而不是只展示未来节点。
* 首批历史范围从 Apollo 8（1968）开始，覆盖 Apollo 11、Apollo 17、LRO、Artemis I / II，以及嫦娥一号、嫦娥三号、嫦娥四号、嫦娥五号、鹊桥二号 / 嫦娥六号等关键已完成步骤。
* 未来节点继续保留计划状态色和来源，包括 Artemis III—V、Gateway 复审、嫦娥七号、嫦娥八号及 2030 年前载人登月目标。
* “时间对齐”按共同的有序日期行实现，不按像素比例制造 1972—2007 的巨大空白；日期是真实的，垂直间距是可读性布局。

### 验收标准

* Starship 不再从看板中消失，也不被错误计入正式载荷发射构型排名。
* 研发卡能直接区分“已经飞过”“已经入轨”“已经回收”和“已进入常态服务”。
* 地月轴至少同时包含中美双方的历史完成事件和未来计划。
* 任一日期行的中美事件共享同一个中央日期标签；空白一侧保留位置，保证桌面端横向比较成立。
* 所有新增状态、里程碑和说明均有中英文版本与官方来源。

---

## 17. 持续公开发布规则

> 状态：已确认，自 Alpha 0.8 起执行。

* 每次形成新的产品版本号后均默认发布到公开站点，包括 major、minor 和 patch / 小版本，不再逐次等待发布确认，以便通过远程地址检查实际效果。
* 默认发布链路为：完成相应验证 → 提交并同步 GitHub → 保存 Sites 版本 → 部署到公开生产站点。
* 仅在用户明确标注“草稿”“仅本地”“不发布”，或验证未通过时暂停公开部署。
* 生产站点继续使用稳定地址 `https://project-cislunar-i.polyguoyang.chatgpt.site/`；历史 Sites 版本保留，用于必要时回退。

---

## 18. 展示层次、运力分类与比例时间轴（Alpha 0.9）

> 状态：已确认并进入实现；按持续公开发布规则上线。

### 发射工业层次与构型分类

* 常规运载数据必须先完整展示：当前 KPI、火箭构型频次和全部时序指标均位于研发飞行之前；研发飞行与工程验证作为发射工业的末级专题，不能抢占正式运载主叙事。
* 火箭构型增加按公开最大有效载荷划分的粗分类：小型 `<2 t`、中型 `2–20 t`、重型 `20–50 t`、超重型 `≥50 t`。优先使用 LEO 最大运力；高轨专用构型可按公开任务包线分类并明确参考轨道。
* 分类只描述构型能力包线，不表示某次任务的实际载荷质量，也不替代任务次数、成功率与入轨质量指标。
* HASTE 等明确的亚轨试验构型单列“亚轨试验”，不伪装为轨道运力级别。
* 朱雀三号遥二在 2026-08-19 完成载荷入轨与一级陆地软着陆，一级回收能力改为“已验证”；项目阶段更新为工程化复用验证。下一门槛是回收箭体检查、维护与首次复飞，不能因一次回收成功直接标为常态服务。

### 地月轨道图谱

* 恢复地月轨道示意，并升级为逐任务轨道图谱；首批同时解释 LRO 月球极轨、鹊桥二号 ELFO、DRO-A/B 远距离逆行轨道和 CAPSTONE NRHO。
* 图形使用响应式代码原生几何示意，突出 POLAR、ELFO、DRO、NRHO 的相对形状与地月关系；不宣称天体尺寸、轨道半径和偏心率按比例。
* 每张图必须绑定具体任务、双语轨道名称、简短特征说明及官方来源；现存地月资产的官方任务图片继续保留在图谱之后。

### 登月计划比例时间轴

* 阿波罗时代从主时间轴移除，当前比较从嫦娥一号（2007）开始，聚焦现代中美地月计划。
* 桌面端改用横向时间轴，并按真实年月距离在 2007—2030 范围内定位节点；美国事件位于轴线上方，中国事件位于下方。
* 同一时期的多个事件使用三层错位卡片避免文字遮挡；时间节点圆点与日期标签分离，任何状态点不得覆盖日期文本。
* 时间轴允许横向滚动，以可读文字换取真实时间比例；窄屏继续保留同一比例画布，而不是退化为等距事件表。

### 验收标准

* 页面 DOM 中研发专题位于全部常规运载图表之后。
* 前六火箭构型均显示运力分类与公开最大运力/参考轨道；分类规则同时在页面可见。
* 朱雀三号显示“一级回收已验证”，同时仍显示“常态载荷待验证”。
* 时间轴首个节点不早于 2007 年，事件横向位置由日期计算，不按事件序号等距排列。
* POLAR、ELFO、DRO、NRHO 四类轨道均有独立任务图和官方来源。

---

## 19. Alpha 1.0 方向提案：从活动看板到工业系统

> 状态：方向讨论稿；已确认原则与候选范围分开记录，尚未开始实现。

### 1.0 的核心问题

Alpha 0.9 已经能回答“有多少在轨资产”“发射了多少”“地月与空间站走到哪一步”。1.0 应进一步回答：

1. 这些资产由谁运营，集中在什么星座和服务上？
2. 在轨系统是在净增长、稳定补充，还是开始老化和退出？
3. 发射流量是否真正转化成持续运营的轨道能力和人类活动？

因此 1.0 的目标不是增加更多炫目的空间视觉，而是把“库存—流量—运营者—更新—前沿活动”连接成可持续追踪的工业系统。

### 已确认原则

* 数字、变化率、原始量和状态追踪继续是产品主体；轨道动力学不是看板本身的研究对象。
* POLAR、ELFO、DRO、NRHO 等轨道图只承担解释任务位置和轨道差异的辅助作用，不扩展成实时轨道动画或动力学模拟器。
* 提升历史长度、数据有效性和口径一致性，优先级高于增加更多视觉动效。
* 数据维护采用本地人工触发的周更新，加上重大事件后的 ad hoc 更新；1.0 不建设端到端无人值守采集和自动生产部署。
* 每次形成新的数据 / 产品版本后仍按持续发布规则公开上线，便于远程检查。

### 建议的 1.0 信息架构

#### A. 总览：工业系统当前状态

* 继续使用中美直接对比，并加入全球基准，不建立综合能力评分。
* 只选择能够指向下游模块的核心原始量：活跃载荷、已知质量、年度新增载荷、净增资产、发射成功任务、已知入轨质量、在轨人员天数、运行中地月资产。
* 每个数字显示数据截止时间、YTD / 年末口径和来源等级。

#### B. 在轨工业基础：库存、运营者与星座（重点新增）

* 在现有在轨数量、质量、类型和轨道分布之外，增加运营者 / 组织排名及其国别归属。
* 增加主要星座规模、年度新增、年度退出、净增长和全球占比；优先覆盖 Starlink、Amazon Leo、中国主要卫星互联网与导航 / 遥感星座。
* 增加运营集中度，但只展示可解释的原始比例，例如前 5 家运营者占全部活跃载荷的百分比，不生成抽象垄断分数。
* 组织归属优先使用实际运营主体；母公司、政府最终所有权等作为补充字段，不混成单一国别推断。

#### C. 资产生命周期与轨道更新能力（重点新增）

* 按年展示新进入轨道、再入 / 退出和净增载荷数量。
* 增加活跃载荷年龄分布、中位年龄和不同任务类型的更新速度。
* 对大型星座增加补网强度：年度新增卫星 ÷ 期初在轨规模。该指标回答系统能否持续维护，而不只是一次性发射多少。
* 将碎片、火箭体和载荷退出作为独立对象类型展示；不把所有再入都解释为运营失败。

#### D. 发射与物流吞吐（现有模块深化）

* 保留发射次数、成功率、构型 / 运力分类和研发火箭分层。
* 1.0 的首要数据升级是把“已知年度入轨质量”和“单次平均已知入轨质量”从产品样例转成任务级可审计数据。
* 复用率、回收成功率、一级周转和飞行轮次只在能够建立任务—一级序列号关联的运营者范围内展示；不能推演时保留空值。
* 平均单位重量入轨成本继续标记为模型值，直到任务价格与实际载荷覆盖达到可接受水平，不把目录报价当作真实成交成本。

#### E. 人类驻留与轨道基础设施（现有空间站模块升级）

* 静态空间站质量、容积、轨道和供电之外，增加年度载人发射、载人到访、货运到访和在轨人员天数。
* “在轨人员天数”作为主时序指标，以每天实际在轨人数积分，比单纯乘组人数更能表达持续驻留能力。
* 继续不维护高频实时乘组名单；人员与飞行事件在周更新或重大任务后复核。

#### F. 地月活动与前沿资产（现有模块深化）

* 保留任务轨道图谱、现存资产和中美比例时间轴。
* 增加按年度统计的地月任务尝试、成功进入目标区域、软着陆、采样返回和载人任务数量。
* 对“运行资产”与“已完成但已失效任务”严格分开；计划节点不能进入已完成任务统计。

#### G. 轨道环境与可持续性（1.0 候选，数据不足时后置）

* 可展示碎片 / 火箭体存量、新增目录对象、年度再入和重大碎裂事件。
* 首版不展示无法稳定复核的实时碰撞概率、规避机动次数或国家级“责任分数”。
* 若数据源覆盖不足，该板块后置到 1.1，不阻塞 1.0。

### 推荐的 1.0 实施优先级

1. 将发射与在轨主历史统一扩展到 2000 年，覆盖早期商业化基线、中国载人航天起点、Falcon 1 / Falcon 9 和星座化之前的阶段。
2. 上线运营者 / 星座结构，以及新增—退出—净增长生命周期时序。
3. 将年度已知入轨质量和平均已知质量转为真实、任务级可审计指标。
4. 把空间站升级为人类驻留模块，上线人员天数和载人 / 货运到访时序。
5. 最后评估轨道环境板块是否达到 1.0 的数据门槛。

### 数据质量门槛

* 1.0 的主视图不再保留无来源的 D 级产品样例；无法升级的数据移至方法页的研究积压区，或暂时隐藏。
* 每个主 KPI 必须具备：来源、截止日期、地区口径、缺失值策略、当前年是否 YTD、历史是否年末快照。
* 任务级数据聚合前保留稳定 ID；运营者、星座、火箭构型和任务类型的归并规则必须可追溯。
* 任一周更新出现超过合理阈值的跳变时先检查源变更、分类变化和重复记录，再发布。

### 本地维护节奏

* 周更新：本地刷新发射、在轨目录和可重算时序，查看 diff，运行一致性测试，通过后发布 patch 版本。
* 重大新闻更新：首次发射、回收 / 复飞、载人任务、地月着陆 / 返回、项目取消或架构调整后，单独复核官方来源并发布小版本。
* 低频审计：在来源版本或分类规则发生变化时重跑完整历史，并记录口径变化；不为固定日历频率强制全量重算。
* 1.0 不部署 cron、无人值守采集服务或自动生产发布。GitHub 继续保存源快照、派生数据、人工审核数据和方法变更。

### 暂不进入 1.0

* 实时轨道位置动画、轨道传播器和交互式动力学模拟。
* 难以统一口径的国家航天预算、企业估值、订单金额和行业收入总分。
* 尚未形成规模化运行的 ISRU、月面能源、生态闭环等指标。
* 无法审计的数据推演型综合能力评分。

### 仍需确认的产品取舍

* 运营者 / 星座与资产生命周期是合并为一个大板块，还是拆为两个连续板块；建议 1.0 合并，以避免导航过长。
* 轨道环境是否进入 1.0 主导航；建议以数据审计结果为准，不作为 1.0 必达项。
* 历史起点是否统一采用 2000 年；建议统一，以获得足够的商业化前基线，同时控制数据清洗范围。

---

## 20. Alpha 1.0 修订：制造、计算与地面发射资产

> 状态：2026-08-20 方向修订；本节优先于第 19 节中“已经升级为工业系统”的表述。

### 概念边界修正

* 只有发射次数、在轨资产、运营者和生命周期，还不足以称为完整“太空工业系统”。这些内容主要描述活动结果、资产库存和运营结构。
* 工业系统至少还需要两类生产性能力：制造能力，以及计算 / 数据处理能力；此外，发射场、发射台和地面保障设施属于进入空间的固定资产，必须纳入发射资产。
* 因此 Alpha 1.0 的准确目标是建立“工业系统观测框架”，而不是宣称已经完整测量太空工业。无法直接观测的制造产能和计算能力必须诚实保留为空白、事件或代理量。

### 1. 地面发射网络（1.0 必达新增板块）

地面发射场是可复用运载器、火箭构型和发射任务共同依赖的固定资产。首批使用 LL2 的 location / pad / mission 关系建立真实资产表。

#### 静态资产

* 轨道级发射场数量、活跃发射台数量、所属国家 / 机构、地理位置和当前状态。
* 每个发射台支持的火箭家族、可达典型轨道、首次 / 最近一次任务和在建升级。
* 发射场、发射台、着陆区、海上回收平台分别建模，不能把一个航天中心简单计为一个 pad。

#### 时序与利用率

* 各发射场 / 发射台年度发射次数、成功任务数和全球占比。
* 最近 12 个月发射次数、发射间隔中位数、最短周转间隔和同时支持的活跃构型数。
* “活跃”以观察窗口内至少一次轨道 / 深空任务为准；在建、停用、亚轨试验和仅着陆设施单独标记。
* 不使用主观“基地能力分数”；用任务量、间隔、构型数和设施状态直接表达产能。

### 2. 制造与交付能力（1.0 核心新增板块）

制造是工业系统成立的必要条件，但大量真实产能不公开。1.0 只展示可审计的实际交付和官方产能披露，不把计划产能当成产量。

#### 可直接观测的制造结果

* 每年交付并进入任务的运载器 / 新造一级数量；可识别序列号时区分新造与复飞。
* 每年进入轨道的载荷对象数量和已知质量，按制造商、运营者、任务类型和国家分组。
* 活跃批量生产火箭家族数量，以及同一构型的年度交付 / 飞行节奏。
* 大型星座年度入轨批次、卫星数量和已知单位质量，作为卫星制造与部署吞吐的可观测结果。

#### 只能作为事件或披露值的制造能力

* 工厂投产、总装线扩建、发动机下线、产能目标和重大供应链节点以带来源事件记录，不自动并入实际产量。
* “设计年产能”“目标月产量”和“已交付数量”必须使用不同字段与状态色。
* 对无法获得序列号的火箭，不反推虚构的新造一级数量；只展示任务 / 构型节奏。

### 3. 轨道计算与数据网络（1.0 探索新增板块）

“Compute”不能简单等同于卫星数量，也不能在芯片和功耗不公开时虚构总 FLOPS。首版以可验证的数据基础设施和计算事件为主。

#### 建议的可观测量

* 宽带通信、数据中继、导航定位和在轨计算 / AI 验证载荷的活跃资产数量与已知质量。
* 公开确认具备星间链路的在轨资产 / 星座，以及光学星间链路、地月中继和自主导航能力的部署事件。
* 数据中继卫星、地面站 / 网关和跨链路服务的资产清单；公开吞吐量时保留原始 Gbps / Tbps，不做无依据汇总。
* 专用在轨计算任务、边缘处理载荷和数据中心概念验证的里程碑时间线。

#### 暂不计算

* 全部在轨卫星总 FLOPS、总存储、AI 算力或“数字文明指数”。
* 用通信带宽直接替代计算能力，或把星座设计容量当作已经可用的实测吞吐。

若首轮数据审计只能支持零散事件，“轨道计算与数据网络”可以先作为状态型专题，不强行生成连续时序图。

### 4. 对 GitHub Issue #1（Alpha 0.4 反馈）的吸收

来源：`https://github.com/carolplay/space-race-dashboard/issues/1`

* **里程碑追踪器**：地月时间轴已在 0.9 实现；1.0 扩展到发射场投运、工厂 / 总装线、批产、星座组网、数据中继和计算验证节点。
* **Capability Lag**：保留为二级派生视图，只用于定义稳定、方向单调、口径一致的原始指标。结果表述为“当前值对应对方哪一历史年份”，不能变成能力总分，也不能用于制造 / 计算披露不完整的指标。
* **阵营与治理版图**：Artemis Accords / ILRS 可作为地月上下文，但不属于工业核心板块，建议 1.1 再评估。
* **地月资产清单**：0.9 已实现首批资产和轨道图谱；1.0 补齐当前运行资产、状态变更和任务结果统计。
* **本期变化**：提升为 1.0 必达功能。每次周更新生成“自上个发布版本以来”的新增任务、新增 / 退出资产、基地活动、里程碑与口径变化。
* **K 指数分解与情景**：继续位于最终结果层，不抢占制造、计算和地面资产的主视图。
* **换算锚点**：可用于提示和解释，但不作为 1.0 的数据建设优先项。

### 修订后的 1.0 顶层板块

1. **总览 / 本期变化**：中美与全球关键原始量，以及上次发布以来的变化。
2. **制造与交付**：火箭、一级、发动机 / 总装事件、卫星交付与星座批量生产结果。
3. **发射网络**：发射场、发射台、支持构型、利用率、周转和常规 / 研发任务。
4. **轨道资产与运营**：库存、质量、运营者、星座、生命周期和净增长。
5. **计算与数据网络**：通信、中继、导航、星间链路和在轨计算验证；按数据成熟度决定时序或状态展示。
6. **人类驻留**：空间站资产、人员天数、载人 / 货运到访。
7. **地月前沿**：运行资产、轨道图谱、任务结果与计划时间轴。
8. **文明结果与方法**：K 指数、证据等级、缺失边界和更新协议。

为了避免首页过长，导航可把 2–3 合并为“生产与发射”，4–5 合并为“轨道基础设施”；页面内部仍保持独立子板块和来源口径。

### 修订后的实施顺序

1. 从现有 LL2 快照派生发射场 / 发射台资产与年度利用率，先建立无需新数据源的地面网络板块。
2. 审计 GCAT 的制造商、运营者、载荷、日期和质量字段，建立实际卫星交付与生命周期序列。
3. 建立制造能力人工审核表，区分实际交付、已投产、官方产能披露和目标计划。
4. 审计通信 / 中继 / 导航分类与星间链路官方披露，决定计算板块能否形成时序。
5. 将 Issue #1 的“本期变化”接到本地周更新流程；最后再加入受约束的 Capability Lag。

---

## 21. Alpha 1.0 实施记录

> 状态：2026-08-20 已实现并通过构建、一致性测试与 lint；公开发布版本为 Alpha 1.0。

### 已上线板块

* **总览 / 本期变化**：增加自上一份 GCAT 快照以来的活跃载荷变化、首次纳入统计的发射台数量，以及“当前中国载荷存量最接近美国哪一历史年份”的受约束 Capability Lag。该派生值只使用同口径载荷库存，不形成综合分数。
* **制造与交付**：使用 GCAT SATCAT 生成 2000—2026 年载荷新增、退役、净变化、已知交付质量与近五年制造商交付排名。企业峰值产能和工厂 / 发动机事件独立显示，不计入实际交付。
* **地面发射网络**：从 LL2 的 mission → pad → location 关系生成活动基地、活动发射台、支持火箭族、首次 / 最近发射、成功率、中位和最短台位间隔。当前覆盖 2024-01-01 至 2026-08-20，识别 27 个发生过轨道发射的基地和 60 个发射台。
* **常规运载与研发飞行**：保留 0.9 的构型、运力级别和试飞分层；主时序只保留 GCAT 可观测的发射尝试、成功任务、已知交付质量和每次成功任务已知交付质量。
* **轨道资产与运营**：历史统一扩展到 2000 年；当前快照增加 GCAT Owner 运营者排名，并保留数量、质量、任务类型、轨道和目录对象组成。
* **计算与数据网络**：以活跃通信 / 导航载荷作为节点视图，并以官方披露的 Starlink 星间激光器、NASA TDRS 和 Near Space Network 地面天线作为链路 / 地面节点信号。明确不汇总带宽或 FLOPS。
* **载人前哨、地月前沿、文明结果与方法**：沿用 0.9 已核验资产和时间轴，按 1.0 导航顺序重排；K 指数继续处于最终结果层。

### 数据升级

* LL2 任务快照增加 pad / location 稳定 ID、名称、状态、经纬度和国别字段，并新增 `data/metrics/launch-infrastructure.json`。
* GCAT 历史默认起点从 2011 改为 2000，新增 `payloadFlow` 与 `recentManufacturers`。
* GCAT Active Catalog 当前快照新增 `byOperator`，并保存 2026-08-20 快照。
* 新增 `data/editorial/industrial-capability.json`，保存双语制造产能披露和数据网络官方信号。
* 0.9 主看板中的无来源复用率、回收率、助推器周转、飞行轮次、年度质量和成本样例曲线已从 1.0 主视图移除；平均单位成本保留为待建立任务价格 / 载荷质量管线的研究项。

### 仍待后续数据建设

* 载人年度发射、到访、货运与在轨人员天数尚未形成可靠连续数据，当前继续采用空间站状态卡。
* 地月任务结果的年度聚合仍需建立独立任务表；当前保留运行资产、轨道图谱和对齐时间轴。
* 火箭新造一级与复飞一级需要可靠序列号关联；在覆盖达标前不展示推算复用率。
* “本期变化”目前比较本地保存的相邻在轨快照；后续周更新再加入任务、基地活动和人工里程碑 diff。
